---
interface Props {
  growth: 'seedling' | 'budding' | 'evergreen';
}

const { growth } = Astro.props;

const badges = {
  seedling: { emoji: '🌱', label: 'Seedling' },
  budding: { emoji: '🌿', label: 'Budding' },
  evergreen: { emoji: '🌳', label: 'Evergreen' },
};

const badge = badges[growth];
---

<span class="growth-badge" data-growth={growth} title={badge.label}>
  <span class="growth-emoji" aria-hidden="true">{badge.emoji}</span>
  <span class="growth-label">{badge.label}</span>
</span>

<style>
  .growth-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-1) var(--space-3);
    border-radius: var(--radius-lg);
    font-size: var(--text-xs);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .growth-badge[data-growth='seedling'] {
    background-color: color-mix(in srgb, var(--color-seedling) 20%, transparent);
    color: var(--color-seedling);
  }

  .growth-badge[data-growth='budding'] {
    background-color: color-mix(in srgb, var(--color-budding) 20%, transparent);
    color: var(--color-budding);
  }

  .growth-badge[data-growth='evergreen'] {
    background-color: color-mix(
      in srgb,
      var(--color-evergreen) 20%,
      transparent
    );
    color: var(--color-evergreen);
  }

  .growth-emoji {
    font-size: var(--text-base);
    line-height: 1;
  }

  [data-theme='dark'] .growth-badge {
    font-weight: 500;
  }
</style>
